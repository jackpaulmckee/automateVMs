plugin: amazon.aws.aws_ec2
regions: [us-west-1]
filters:
  tag:environment: test

compose:
  ansible_host: public_ip_address
  aws_tags: tags

keyed_groups:
  - key: tags.environment
    prefix: ec2xhosts
    separator: "_"